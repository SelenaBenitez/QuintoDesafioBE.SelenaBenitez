const chai = require('chai');
const expect = chai.expect;
const request = require('supertest');
const app = require('../app'); // Asegúrate de que la ruta sea correcta

describe('Router de sesiones', () => {
  it('Debería iniciar sesión con credenciales válidas', (done) => {
    // Escribe una prueba para iniciar sesión con credenciales válidas
    done();
  });

  // Agrega más pruebas relacionadas con sesiones si es necesario
});
