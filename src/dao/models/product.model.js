class Product {
  constructor(name, description, price, ownerId) {
    this.name = name;
    this.description = description;
    this.price = price;
    // ... otros campos que puedas tener
    this.ownerId = ownerId;
  }
}

export default Product;
